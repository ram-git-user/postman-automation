name: Test Self Hosted Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      - self-hosted
      - Windows
      - X64

    steps:
      - name: Display System Info
        run: |
          echo "Runner is working!"
          hostname
          whoami
          pwd
